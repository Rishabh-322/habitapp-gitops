apiVersion: v1
kind: Service
metadata:
  name: habit-app

spec:
  type: {{ .Values.service.type }}

  selector:
    app: habit-app

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP